# Programs that require a desktop manager or graphics to run
# If a program is uncommon, it will be installed from requires-ppa.txt

# Text editor similar to notepad in Windows
gedit

# Used for graphical sudo password input
gksu

# Program to work with discs for different types (burning, ripping, backups)
brasero

